##### 1、web 8000端口的各种应用不连接

```bash
#重启服务
sudo nginx
```



##### 2、web端 ubuntu Desktop 不在桌面上，在web端更新Aid-Desktop软件即可





##### 3、使用QC6490开发板遇到的问题

```shell
1、文档中刷机完后下载各种软件使用的是有线网，但是还可以使用电脑或者手机连接上APLUXwifi之后再打开热点，将板子、手机、电脑在一个热点下面，这样就是基于APLUX的局域网，也在APLUX内网下，可以减少有线网连接

--- 下载文件时最好使用有线网，更加稳定和快速。目前板子的有线网口还存在问题，正在解决中：1、左边网口即eth0 的mac地址是写死了的，所有板子都是同一个mac。因此大家在使用时会导致IP冲突。 2、右边网口即eth1的mac是随机的，即每个板子每次重启都会随机生成一个mac，就会导致IP地址发生变化。 如果你们想用稳定的ip地址，可以尝试使用wifi连接。可以直接找刘维斌看能否给大家申请天线，就可以直接连接APLUX网络了。

 
 
2、板子的VNC服务不稳定，不知道什么原因，有的是拒绝，有的是超时，暂时不知道原因。Web8000端口也是只能被动显示（比如代码中的cv.imshow），不可以主动显示，比如不能打开rviz2，报错没有显示器。可以使用Xshell套装，盗版或者是30天免费使用就可以，基于ssh的主动显示。
 另外方法：ros2使用的是tcp通信，可以使用linux系统连接那个节点，但是需要路由器带动带宽

---vnc连接不稳定应该跟网络有关，如连接的wifi，可能导致网络不稳当，vnc就会断开。 Web8000上确认是不能打开rviz2的；可通过xshell、mobaxterm（有免费社区版）等调用本地资源打开。

 

3、ubuntu系统中使用设备权限问题，在ubuntu中使用设备需要加上sudo命令，一旦涉及到硬件（目前发现是usb耳机音响），如果仅仅是查看设备信息，有些都需要sudo

--当前系统默认进入的都是aidlux用户，它仅在/home/aidlux目录下有操作权限。一些系统命令或者去其他目录下操作都应当加sudo的。没有问题。

 

4、板子有时不知道为什么直接断电，可能是死机原因

--更换为最新固件，蓝灯版本

 

5、Aidlux的所有软件拒绝连接，比如终端、应用中心等

--sudo nginx重启一下服务
 

 
6、VNC中打开VS Code 在尝试创建沙盒环境时遇到了权限问题（Operation not permitted），通常与 Linux 系统的命名空间（namespace）限制或安全策略有关。以下是解决方案：

 禁用沙盒模式（推荐）

直接运行以下命令绕过沙盒检查：

/usr/share/code/bin/code --no-sandbox --user-data-dir=~/.vscode-unsandboxed
```

